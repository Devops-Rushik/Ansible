---
- hosts: all
  become: yes
  gather_facts: no
  tasks:
   - name: copy script to remote server
     copy:
      src: /home/rushik-devops/precheck.sh
      dest: /tmp/
      owner:  root
      group: root
      mode: u+rwx,g+rwx,o+rwx
   - name: run the script
     shell: /tmp/precheck.sh
